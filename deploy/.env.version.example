# Copy to deploy/.env or export before docker compose build/up.
# Bump this value for every deployment so System Status does not show stale component versions.
ARGUS_ENGINE_VERSION=2.6.2
BUILD_SOURCE_STAMP=local-2.6.2
