# Optional manual override for direct docker compose usage.
# deploy.py also patches explicit scales through deploy.py scale local.
services:
  worker-spider:
    deploy:
      replicas: 1
  worker-http-requester:
    deploy:
      replicas: 1
  worker-enum:
    deploy:
      replicas: 1
  worker-portscan:
    deploy:
      replicas: 1
  worker-highvalue:
    deploy:
      replicas: 1
  worker-techid:
    deploy:
      replicas: 1
